// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/0x587/guardeye/common/ent/subscribe"
)

// Subscribe is the model entity for the Subscribe schema.
type Subscribe struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Enable holds the value of the "enable" field.
	Enable bool `json:"enable,omitempty"`
	// Query holds the value of the "query" field.
	Query string `json:"query,omitempty"`
	// Type holds the value of the "type" field.
	Type subscribe.Type `json:"type,omitempty"`
	// WebHookURL holds the value of the "web_hook_url" field.
	WebHookURL string `json:"web_hook_url,omitempty"`
	// MqttPushURL holds the value of the "mqtt_push_url" field.
	MqttPushURL string `json:"mqtt_push_url,omitempty"`
	// MqttPushTopic holds the value of the "mqtt_push_topic" field.
	MqttPushTopic string `json:"mqtt_push_topic,omitempty"`
	// WebSocketKey holds the value of the "web_socket_key" field.
	WebSocketKey string `json:"web_socket_key,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Subscribe) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case subscribe.FieldEnable:
			values[i] = new(sql.NullBool)
		case subscribe.FieldID:
			values[i] = new(sql.NullInt64)
		case subscribe.FieldQuery, subscribe.FieldType, subscribe.FieldWebHookURL, subscribe.FieldMqttPushURL, subscribe.FieldMqttPushTopic, subscribe.FieldWebSocketKey:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Subscribe fields.
func (s *Subscribe) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case subscribe.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			s.ID = int(value.Int64)
		case subscribe.FieldEnable:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enable", values[i])
			} else if value.Valid {
				s.Enable = value.Bool
			}
		case subscribe.FieldQuery:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field query", values[i])
			} else if value.Valid {
				s.Query = value.String
			}
		case subscribe.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				s.Type = subscribe.Type(value.String)
			}
		case subscribe.FieldWebHookURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field web_hook_url", values[i])
			} else if value.Valid {
				s.WebHookURL = value.String
			}
		case subscribe.FieldMqttPushURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mqtt_push_url", values[i])
			} else if value.Valid {
				s.MqttPushURL = value.String
			}
		case subscribe.FieldMqttPushTopic:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mqtt_push_topic", values[i])
			} else if value.Valid {
				s.MqttPushTopic = value.String
			}
		case subscribe.FieldWebSocketKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field web_socket_key", values[i])
			} else if value.Valid {
				s.WebSocketKey = value.String
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Subscribe.
// This includes values selected through modifiers, order, etc.
func (s *Subscribe) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// Update returns a builder for updating this Subscribe.
// Note that you need to call Subscribe.Unwrap() before calling this method if this Subscribe
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Subscribe) Update() *SubscribeUpdateOne {
	return NewSubscribeClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Subscribe entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Subscribe) Unwrap() *Subscribe {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("ent: Subscribe is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Subscribe) String() string {
	var builder strings.Builder
	builder.WriteString("Subscribe(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("enable=")
	builder.WriteString(fmt.Sprintf("%v", s.Enable))
	builder.WriteString(", ")
	builder.WriteString("query=")
	builder.WriteString(s.Query)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", s.Type))
	builder.WriteString(", ")
	builder.WriteString("web_hook_url=")
	builder.WriteString(s.WebHookURL)
	builder.WriteString(", ")
	builder.WriteString("mqtt_push_url=")
	builder.WriteString(s.MqttPushURL)
	builder.WriteString(", ")
	builder.WriteString("mqtt_push_topic=")
	builder.WriteString(s.MqttPushTopic)
	builder.WriteString(", ")
	builder.WriteString("web_socket_key=")
	builder.WriteString(s.WebSocketKey)
	builder.WriteByte(')')
	return builder.String()
}

// Subscribes is a parsable slice of Subscribe.
type Subscribes []*Subscribe
