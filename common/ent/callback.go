// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/0x587/guardeye/common/ent/callback"
	"github.com/google/uuid"
)

// Callback is the model entity for the Callback schema.
type Callback struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ClientID holds the value of the "client_id" field.
	ClientID uuid.UUID `json:"client_id,omitempty"`
	// CreateAt holds the value of the "create_at" field.
	CreateAt time.Time `json:"create_at,omitempty"`
	// UpdateAt holds the value of the "update_at" field.
	UpdateAt time.Time `json:"update_at,omitempty"`
	// Cfg holds the value of the "cfg" field.
	Cfg          string `json:"cfg,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Callback) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case callback.FieldID:
			values[i] = new(sql.NullInt64)
		case callback.FieldCfg:
			values[i] = new(sql.NullString)
		case callback.FieldCreateAt, callback.FieldUpdateAt:
			values[i] = new(sql.NullTime)
		case callback.FieldClientID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Callback fields.
func (c *Callback) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case callback.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = int(value.Int64)
		case callback.FieldClientID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field client_id", values[i])
			} else if value != nil {
				c.ClientID = *value
			}
		case callback.FieldCreateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_at", values[i])
			} else if value.Valid {
				c.CreateAt = value.Time
			}
		case callback.FieldUpdateAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_at", values[i])
			} else if value.Valid {
				c.UpdateAt = value.Time
			}
		case callback.FieldCfg:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cfg", values[i])
			} else if value.Valid {
				c.Cfg = value.String
			}
		default:
			c.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Callback.
// This includes values selected through modifiers, order, etc.
func (c *Callback) Value(name string) (ent.Value, error) {
	return c.selectValues.Get(name)
}

// Update returns a builder for updating this Callback.
// Note that you need to call Callback.Unwrap() before calling this method if this Callback
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Callback) Update() *CallbackUpdateOne {
	return NewCallbackClient(c.config).UpdateOne(c)
}

// Unwrap unwraps the Callback entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Callback) Unwrap() *Callback {
	_tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Callback is not a transactional entity")
	}
	c.config.driver = _tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Callback) String() string {
	var builder strings.Builder
	builder.WriteString("Callback(")
	builder.WriteString(fmt.Sprintf("id=%v, ", c.ID))
	builder.WriteString("client_id=")
	builder.WriteString(fmt.Sprintf("%v", c.ClientID))
	builder.WriteString(", ")
	builder.WriteString("create_at=")
	builder.WriteString(c.CreateAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_at=")
	builder.WriteString(c.UpdateAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("cfg=")
	builder.WriteString(c.Cfg)
	builder.WriteByte(')')
	return builder.String()
}

// Callbacks is a parsable slice of Callback.
type Callbacks []*Callback
